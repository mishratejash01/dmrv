# Never upload secrets or local-only tooling to the Vercel build.
# NOTE: leading "/" anchors to repo root so we do NOT accidentally exclude
# src/lib/supabase/* (only the top-level supabase/ migrations folder).
/.env
/.env.*
/scripts/
/supabase/
/.claude/
/scratchpad/
*.log
